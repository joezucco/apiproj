const About = (props) => {
  return (
    <div>
      <h2>About</h2>
      <p>Catfax is a database of (relatively) true information about cats. In fact, this entire app was built by an actual cat!</p>
    </div>
  );
};

export default About;
