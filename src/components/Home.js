import { Link } from "react-router-dom"
import Plant from "./Plant"

const Home = (props) => {
    return (
    <div>
        <h2>Home</h2>
        <Plant />
        </div>
    )
}

export default Home